/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/docs/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import { CryptoTokenService } from "../cryptoToken.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { CryptoTokenCreateInput } from "./CryptoTokenCreateInput";
import { CryptoTokenWhereInput } from "./CryptoTokenWhereInput";
import { CryptoTokenWhereUniqueInput } from "./CryptoTokenWhereUniqueInput";
import { CryptoTokenFindManyArgs } from "./CryptoTokenFindManyArgs";
import { CryptoTokenUpdateInput } from "./CryptoTokenUpdateInput";
import { CryptoToken } from "./CryptoToken";
@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class CryptoTokenControllerBase {
  constructor(
    protected readonly service: CryptoTokenService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @nestAccessControl.UseRoles({
    resource: "CryptoToken",
    action: "create",
    possession: "any",
  })
  @common.Post()
  @swagger.ApiCreatedResponse({ type: CryptoToken })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async create(
    @common.Body() data: CryptoTokenCreateInput
  ): Promise<CryptoToken> {
    return await this.service.create({
      data: {
        ...data,

        financialAssets: data.financialAssets
          ? {
              connect: data.financialAssets,
            }
          : undefined,
      },
      select: {
        createdAt: true,

        financialAssets: {
          select: {
            id: true,
          },
        },

        id: true,
        updatedAt: true,
        value: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @nestAccessControl.UseRoles({
    resource: "CryptoToken",
    action: "read",
    possession: "any",
  })
  @common.Get()
  @swagger.ApiOkResponse({ type: [CryptoToken] })
  @swagger.ApiForbiddenResponse()
  @ApiNestedQuery(CryptoTokenFindManyArgs)
  async findMany(@common.Req() request: Request): Promise<CryptoToken[]> {
    const args = plainToClass(CryptoTokenFindManyArgs, request.query);
    return this.service.findMany({
      ...args,
      select: {
        createdAt: true,

        financialAssets: {
          select: {
            id: true,
          },
        },

        id: true,
        updatedAt: true,
        value: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @nestAccessControl.UseRoles({
    resource: "CryptoToken",
    action: "read",
    possession: "own",
  })
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: CryptoToken })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async findOne(
    @common.Param() params: CryptoTokenWhereUniqueInput
  ): Promise<CryptoToken | null> {
    const result = await this.service.findOne({
      where: params,
      select: {
        createdAt: true,

        financialAssets: {
          select: {
            id: true,
          },
        },

        id: true,
        updatedAt: true,
        value: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @nestAccessControl.UseRoles({
    resource: "CryptoToken",
    action: "update",
    possession: "any",
  })
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: CryptoToken })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async update(
    @common.Param() params: CryptoTokenWhereUniqueInput,
    @common.Body() data: CryptoTokenUpdateInput
  ): Promise<CryptoToken | null> {
    try {
      return await this.service.update({
        where: params,
        data: {
          ...data,

          financialAssets: data.financialAssets
            ? {
                connect: data.financialAssets,
              }
            : undefined,
        },
        select: {
          createdAt: true,

          financialAssets: {
            select: {
              id: true,
            },
          },

          id: true,
          updatedAt: true,
          value: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @nestAccessControl.UseRoles({
    resource: "CryptoToken",
    action: "delete",
    possession: "any",
  })
  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: CryptoToken })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async delete(
    @common.Param() params: CryptoTokenWhereUniqueInput
  ): Promise<CryptoToken | null> {
    try {
      return await this.service.delete({
        where: params,
        select: {
          createdAt: true,

          financialAssets: {
            select: {
              id: true,
            },
          },

          id: true,
          updatedAt: true,
          value: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
